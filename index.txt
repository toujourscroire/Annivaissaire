<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Joyeux anniversaire ma puce ❤️</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;600;700&family=Montserrat:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        /* ========================================
           VARIABLES CSS
           ======================================== */
        :root {
            --primary: #ff6b9d;
            --secondary: #ffc1cc;
            --accent: #ff1744;
            --dark: #2d132c;
            --light: #fff5f7;
            --gold: #ffd700;
            --purple: #c77dff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(-45deg, #ff9a9e, #fad0c4, #fbc2eb, #a18cd1);
            background-size: 400% 400%;
            animation: gradientFlow 15s ease infinite;
            color: var(--dark);
            overflow-x: hidden;
            position: relative;
            min-height: 100vh;
        }

        @keyframes gradientFlow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* ========================================
           FLOATING PARTICLES
           ======================================== */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            width: 8px;
            height: 8px;
            background: rgba(255, 255, 255, 0.6);
            border-radius: 50%;
            animation: float 20s infinite ease-in-out;
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0) translateX(0);
                opacity: 0;
            }
            10% { opacity: 1; }
            90% { opacity: 1; }
            100% {
                transform: translateY(-100vh) translateX(100px);
                opacity: 0;
            }
        }

        /* ========================================
           CONTAINER
           ======================================== */
        .container {
            position: relative;
            z-index: 2;
            max-width: 1400px;
            margin: 0 auto;
            padding: 60px 20px;
        }

        /* ========================================
           HEADER
           ======================================== */
        header {
            text-align: center;
            margin-bottom: 60px;
            animation: fadeInDown 1.2s ease-out;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h1 {
            font-family: 'Dancing Script', cursive;
            font-size: clamp(2.5rem, 8vw, 6rem);
            font-weight: 700;
            color: white;
            text-shadow: 
                3px 3px 6px rgba(255, 23, 68, 0.4),
                0 0 30px rgba(255, 107, 157, 0.6);
            margin-bottom: 20px;
            letter-spacing: 2px;
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from {
                text-shadow: 
                    3px 3px 6px rgba(255, 23, 68, 0.4),
                    0 0 30px rgba(255, 107, 157, 0.6);
            }
            to {
                text-shadow: 
                    3px 3px 6px rgba(255, 23, 68, 0.6),
                    0 0 50px rgba(255, 107, 157, 0.9);
            }
        }

        .subtitle {
            font-size: clamp(1.2rem, 3vw, 2rem);
            color: white;
            font-weight: 300;
            font-style: italic;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }

        /* ========================================
           DAY COUNTER
           ======================================== */
        .counter-section {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 40px;
            padding: 50px 40px;
            text-align: center;
            margin-bottom: 60px;
            box-shadow: 
                0 25px 70px rgba(255, 107, 157, 0.25),
                inset 0 0 30px rgba(255, 255, 255, 0.5);
            border: 3px solid rgba(255, 255, 255, 0.8);
            animation: fadeInUp 1s ease-out 0.3s both;
            position: relative;
            overflow: hidden;
        }

        .counter-section::before {
            content: '🎂';
            position: absolute;
            top: -20px;
            right: -20px;
            font-size: 100px;
            opacity: 0.1;
            transform: rotate(-15deg);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .counter-label {
            font-size: 1.6rem;
            color: var(--primary);
            margin-bottom: 25px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .counter-display {
            font-family: 'Dancing Script', cursive;
            font-size: clamp(4rem, 12vw, 8rem);
            font-weight: 700;
            background: linear-gradient(135deg, var(--accent), var(--primary), var(--purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(2px 2px 4px rgba(255, 23, 68, 0.3));
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .counter-unit {
            font-size: 1.8rem;
            color: var(--purple);
            margin-top: 15px;
            font-weight: 600;
        }

        /* ========================================
           PHOTO GALLERY
           ======================================== */
        .gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            margin-bottom: 60px;
        }

        .photo-card {
            position: relative;
            border-radius: 30px;
            overflow: hidden;
            box-shadow: 
                0 20px 60px rgba(255, 107, 157, 0.3),
                inset 0 0 20px rgba(255, 255, 255, 0.2);
            transition: all 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
            animation: fadeInUp 1s ease-out both;
            background: white;
            aspect-ratio: 3/4;
            border: 4px solid rgba(255, 255, 255, 0.9);
        }

        .photo-card:nth-child(1) { animation-delay: 0.5s; }
        .photo-card:nth-child(2) { animation-delay: 0.7s; }
        .photo-card:nth-child(3) { animation-delay: 0.9s; }
        .photo-card:nth-child(4) { animation-delay: 1.1s; }
        .photo-card:nth-child(5) { animation-delay: 1.3s; }

        .photo-card:hover {
            transform: translateY(-20px) scale(1.05) rotate(2deg);
            box-shadow: 
                0 30px 80px rgba(255, 107, 157, 0.5),
                inset 0 0 30px rgba(255, 255, 255, 0.3);
        }

        .photo-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }

        .photo-placeholder {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            font-size: 6rem;
        }

        .photo-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(to top, rgba(255, 23, 68, 0.95), transparent);
            color: white;
            padding: 40px 20px 20px;
            font-size: 1.4rem;
            font-weight: 600;
            text-align: center;
            transform: translateY(100%);
            transition: transform 0.5s ease;
            font-family: 'Dancing Script', cursive;
        }

        .photo-card:hover .photo-overlay {
            transform: translateY(0);
        }

        /* ========================================
           SURPRISE BUTTON
           ======================================== */
        .surprise-section {
            text-align: center;
            margin-bottom: 60px;
            animation: fadeInUp 1s ease-out 1.5s both;
        }

        .surprise-button {
            position: relative;
            background: linear-gradient(135deg, var(--accent), var(--primary), var(--purple));
            color: white;
            border: none;
            padding: 30px 70px;
            font-size: 1.8rem;
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            border-radius: 60px;
            cursor: pointer;
            box-shadow: 
                0 20px 50px rgba(255, 23, 68, 0.5),
                inset 0 0 20px rgba(255, 255, 255, 0.3);
            transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
            overflow: hidden;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .surprise-button::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.4);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }

        .surprise-button:hover {
            transform: translateY(-8px) scale(1.08);
            box-shadow: 
                0 30px 70px rgba(255, 23, 68, 0.7),
                inset 0 0 30px rgba(255, 255, 255, 0.4);
        }

        .surprise-button:hover::before {
            width: 400px;
            height: 400px;
        }

        /* ========================================
           HEARTS ANIMATION
           ======================================== */
        .hearts-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 9998;
            overflow: hidden;
        }

        .animated-heart {
            position: absolute;
            font-size: 50px;
            opacity: 0;
            pointer-events: none;
            animation: heartRise 4s ease-out forwards;
        }

        @keyframes heartRise {
            0% {
                opacity: 0;
                transform: translateY(0) scale(0) rotate(0deg);
            }
            20% { opacity: 1; }
            100% {
                opacity: 0;
                transform: translateY(-400px) scale(1.8) rotate(360deg);
            }
        }

        /* ========================================
           MESSAGE OVERLAY
           ======================================== */
        .message-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 107, 157, 0.95);
            backdrop-filter: blur(20px);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            animation: fadeIn 0.8s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .message-overlay.active {
            display: flex;
        }

        .message-content {
            background: white;
            border-radius: 40px;
            padding: 60px 50px;
            max-width: 700px;
            width: 90%;
            box-shadow: 0 30px 100px rgba(0, 0, 0, 0.3);
            animation: scaleIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
            position: relative;
            text-align: center;
        }

        @keyframes scaleIn {
            from {
                transform: scale(0.3);
                opacity: 0;
            }
            to {
                transform: scale(1);
                opacity: 1;
            }
        }

        .message-content h2 {
            font-family: 'Dancing Script', cursive;
            font-size: clamp(2rem, 6vw, 4rem);
            color: var(--accent);
            margin-bottom: 30px;
            animation: glow 2s ease-in-out infinite alternate;
        }

        .message-content p {
            font-size: 1.4rem;
            line-height: 1.8;
            color: var(--dark);
            margin-bottom: 20px;
        }

        .close-button {
            position: absolute;
            top: 20px;
            right: 20px;
            background: var(--accent);
            color: white;
            border: none;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            font-size: 1.8rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(255, 23, 68, 0.4);
        }

        .close-button:hover {
            transform: rotate(90deg) scale(1.1);
            box-shadow: 0 8px 25px rgba(255, 23, 68, 0.6);
        }

        /* ========================================
           CONFETTI
           ======================================== */
        .confetti {
            position: fixed;
            width: 10px;
            height: 10px;
            background: var(--gold);
            position: absolute;
            animation: confettiFall 3s ease-out forwards;
        }

        @keyframes confettiFall {
            to {
                transform: translateY(100vh) rotate(360deg);
                opacity: 0;
            }
        }

        /* ========================================
           RESPONSIVE
           ======================================== */
        @media (max-width: 768px) {
            .container {
                padding: 40px 15px;
            }

            .counter-section {
                padding: 35px 25px;
            }

            .gallery {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .surprise-button {
                padding: 25px 50px;
                font-size: 1.4rem;
            }

            .message-content {
                padding: 40px 30px;
            }

            .message-content h2 {
                font-size: 2.5rem;
            }

            .message-content p {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <!-- Particles Background -->
    <div class="particles" id="particles"></div>

    <!-- Hearts Container -->
    <div class="hearts-container" id="heartsContainer"></div>

    <!-- Main Container -->
    <div class="container">
        <!-- Header -->
        <header>
            <h1>Joyeux anniversaire ma puce ❤️</h1>
            <p class="subtitle">Ma puce, mon coeur,Valéééééé ohhh ✨</p>
        </header>

        <!-- Day Counter -->
        <div class="counter-section">
            <div class="counter-label">Ensemble depuis</div>
            <div class="counter-display">4</div>
            <div class="counter-unit">mois de pur palabre MDR 💕</div>
        </div>

        <!-- Photo Gallery -->
        <div class="gallery">
            <div class="photo-card">
                <img src="photo1.jpg" alt="Photo 1" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                <div class="photo-placeholder" style="display: none;">📸</div>
                <div class="photo-overlay">Notre Premier Moment</div>
            </div>
            <div class="photo-card">
                <img src="photo2.jpg" alt="Photo 2" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                <div class="photo-placeholder" style="display: none;">💑</div>
                <div class="photo-overlay">Toi & Moi</div>
            </div>
            <div class="photo-card">
                <img src="photo3.jpg" alt="Photo 3" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                <div class="photo-placeholder" style="display: none;">💖</div>
                <div class="photo-overlay">Un Instant Magique</div>
            </div>
            <div class="photo-card">
                <img src="photo4.jpg" alt="Photo 4" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                <div class="photo-placeholder" style="display: none;">🌹</div>
                <div class="photo-overlay">Souvenirs Précieux</div>
            </div>
            <div class="photo-card">
                <img src="photo5.jpg" alt="Photo 5" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                <div class="photo-placeholder" style="display: none;">💝</div>
                <div class="photo-overlay">Pour Toujours</div>
            </div>
        </div>

        <!-- Surprise Button -->
        <div class="surprise-section">
            <button class="surprise-button" id="surpriseButton">
                🎁 Clique ici pour ta surprise
            </button>
        </div>
    </div>

    <!-- Message Overlay -->
    <div class="message-overlay" id="messageOverlay">
        <div class="message-content">
            <button class="close-button" id="closeButton">×</button>
            <h2>Mon amour pour toi 💖</h2>
            <p>
                Joyeux anniversaire bébé, j’espère que mon message te trouvera en bonne santé,
                 que ce jour soit rempli de belles choses et de bonnes ondes,
                 je sais qu’on ne pourra pas se voir jusqu’au mois prochain
                 mais sache que même de loin je pense à toi, encore une fois 
                 merci pour tous ces moments, pour tout ton amour que tu me donnes
                 , certes tu penses que ce n’est pas assez mais cela me comble, 
                 joyeux anniversaire mon cœur ❤️ ❤️💕💖
            </p>
        </div>
    </div>

    <script>
        // ========================================
        // BACKGROUND PARTICLES
        // ========================================
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            
            for (let i = 0; i < 50; i++) {
                setTimeout(() => {
                    const particle = document.createElement('div');
                    particle.className = 'particle';
                    particle.style.left = Math.random() * 100 + '%';
                    particle.style.animationDelay = Math.random() * 5 + 's';
                    particle.style.animationDuration = (Math.random() * 10 + 15) + 's';
                    particlesContainer.appendChild(particle);
                }, i * 100);
            }
        }

        createParticles();

        // ========================================
        // SURPRISE BUTTON - HEARTS & MESSAGE
        // ========================================
        const surpriseButton = document.getElementById('surpriseButton');
        const messageOverlay = document.getElementById('messageOverlay');
        const closeButton = document.getElementById('closeButton');
        const heartsContainer = document.getElementById('heartsContainer');

        surpriseButton.addEventListener('click', function(e) {
            // Create heart explosion
            createHeartExplosion(e.pageX, e.pageY);
            
            // Show message overlay
            setTimeout(() => {
                messageOverlay.classList.add('active');
                createConfetti();
            }, 1000);
            
            // Change button text
            surpriseButton.innerHTML = '💝 Je t\'aime mon amour !';
        });

        // Close message
        closeButton.addEventListener('click', function() {
            messageOverlay.classList.remove('active');
        });

        messageOverlay.addEventListener('click', function(e) {
            if (e.target === messageOverlay) {
                messageOverlay.classList.remove('active');
            }
        });

        // ========================================
        // HEARTS EXPLOSION
        // ========================================
        function createHeartExplosion(x, y) {
            const heartEmojis = ['❤️', '💕', '💖', '💗', '💝', '💞', '💓', '💘', '💟', '♥️'];
            const numHearts = 40;

            for (let i = 0; i < numHearts; i++) {
                setTimeout(() => {
                    const heart = document.createElement('div');
                    heart.className = 'animated-heart';
                    heart.textContent = heartEmojis[Math.floor(Math.random() * heartEmojis.length)];
                    
                    const offsetX = (Math.random() - 0.5) * 300;
                    const offsetY = (Math.random() - 0.5) * 100;
                    heart.style.left = (x + offsetX) + 'px';
                    heart.style.top = (y + offsetY) + 'px';
                    heart.style.fontSize = (Math.random() * 40 + 30) + 'px';
                    heart.style.animationDelay = (Math.random() * 0.5) + 's';
                    
                    heartsContainer.appendChild(heart);
                    
                    setTimeout(() => {
                        heart.remove();
                    }, 4000);
                }, i * 60);
            }
        }

        // ========================================
        // CONFETTI
        // ========================================
        function createConfetti() {
            const colors = ['#ff6b9d', '#ffc1cc', '#ff1744', '#ffd700', '#c77dff'];
            
            for (let i = 0; i < 100; i++) {
                setTimeout(() => {
                    const confetti = document.createElement('div');
                    confetti.className = 'confetti';
                    confetti.style.left = Math.random() * 100 + '%';
                    confetti.style.top = '-10px';
                    confetti.style.background = colors[Math.floor(Math.random() * colors.length)];
                    confetti.style.width = (Math.random() * 10 + 5) + 'px';
                    confetti.style.height = (Math.random() * 10 + 5) + 'px';
                    confetti.style.animationDelay = (Math.random() * 0.5) + 's';
                    confetti.style.animationDuration = (Math.random() * 2 + 2) + 's';
                    
                    document.body.appendChild(confetti);
                    
                    setTimeout(() => {
                        confetti.remove();
                    }, 3500);
                }, i * 30);
            }
        }
    </script>
</body>
</html>
